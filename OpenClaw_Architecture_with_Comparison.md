# OpenClaw 技术架构详解（含与 OpenCode 对比）

**文档定位**：OpenClaw 为主，OpenCode 为辅的架构对比分析  
**版本**：v1.0  
**更新日期**：2026年2月2日

---

## 目录

1. [OpenClaw 概述](#一openclaw-概述)
2. [七层系统架构](#二七层系统架构)
3. [核心组件详解](#三核心组件详解)
4. [完整数据流](#四完整数据流)
5. [与 OpenCode 架构对比](#五与-opencode-架构对比)
6. [技术选型建议](#六技术选型建议)

---

## 一、OpenClaw 概述

### 1.1 项目定位

**OpenClaw** 是一个开源的**本地化 AI 智能助手平台**，运行在企业自有服务器上，通过 WhatsApp、钉钉、微信等通讯工具与用户交互，并能自动操作数据库、浏览器、文件系统等完成复杂任务。

**核心理念**：
> "Your assistant. Your machine. Your rules."

### 1.2 技术栈

```yaml
语言: TypeScript
运行时: Node.js 22+
架构: 多进程微服务
进程模型:
  - Gateway进程（端口18789）
  - Agent进程（核心业务）
  - Cron进程（定时任务，可选）
存储:
  - SQLite（会话、记忆）
  - JSON（配置）
  - 向量数据库（语义搜索）
通讯:
  - WebSocket（实时）
  - HTTP REST API
  - IPC（进程间通信）
```

---

## 二、七层系统架构

### 2.1 总体架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              第1层：用户接入层                               │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ WhatsApp │  │ Telegram │  │  Discord │  │   钉钉   │  │   邮件   │      │
│  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘  └────┬─────┘      │
└───────┼─────────────┼─────────────┼─────────────┼─────────────┼────────────┘
        │             │             │             │             │
        └─────────────┴─────────────┴──────┬──────┴─────────────┴────────────┘
                                           │
                                    Webhook / WebSocket
                                           │
┌──────────────────────────────────────────┼──────────────────────────────────┐
│                         第2层：网关层 (Gateway Layer)                          │
│  ┌───────────────────────────────────────┼──────────────────────────────┐   │
│  │        OpenClaw Gateway (Port 18789)  │                              │   │
│  │  ┌─────────────┐  ┌─────────────┐     │    ┌─────────────┐          │   │
│  │  │  HTTP API   │  │ WebSocket   │◄────┴───►│  Channel    │          │   │
│  │  │   Server    │  │   Server    │          │  Adapters   │          │   │
│  │  └──────┬──────┘  └──────┬──────┘          └──────┬──────┘          │   │
│  │         │                │                        │                 │   │
│  │  ┌──────▼────────────────▼────────────────────────▼──────┐           │   │
│  │  │                  认证与路由层                         │           │   │
│  │  │  • Token 验证  • 白名单检查  • 消息路由              │           │   │
│  │  └───────────────────────┬──────────────────────────────┘           │   │
│  └──────────────────────────┼──────────────────────────────────────────┘   │
└─────────────────────────────┼──────────────────────────────────────────────┘
                              │
                              │ 进程间通信 (IPC)
                              │
┌─────────────────────────────▼──────────────────────────────────────────────┐
│                        第3层：核心服务层 (Core Service Layer)                 │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Session Manager                              │   │
│  │   • 会话创建/查找  • 上下文维护  • 生命周期管理                        │   │
│  └──────────────────────────────┬──────────────────────────────────────┘   │
│                                 │                                           │
│  ┌──────────────────────────────▼──────────────────────────────────────┐   │
│  │                        Memory System                                 │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐              │   │
│  │  │ 短期记忆     │  │ 长期记忆     │  │ 语义记忆     │              │   │
│  │  │ (对话历史)   │  │ (用户偏好)   │  │ (向量检索)   │              │   │
│  │  └──────────────┘  └──────────────┘  └──────────────┘              │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Cron Scheduler                               │   │
│  │   • 定时任务调度  • Heartbeat 轮询  • 任务队列管理                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────┬──────────────────────────────────────────┘
                                  │
                                  │ LLM API 调用
                                  │
┌─────────────────────────────────▼──────────────────────────────────────────┐
│                         第4层：AI 引擎层 (AI Engine Layer)                    │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │    Kimi      │  │   Claude     │  │    GPT-4     │  │   本地模型   │   │
│  │ (Moonshot)   │  │ (Anthropic)  │  │  (OpenAI)    │  │  (Ollama)    │   │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘   │
│         │                 │                 │                 │           │
│         └─────────────────┴─────────┬───────┴─────────────────┘           │
│                                     │                                     │
│  ┌──────────────────────────────────▼──────────────────────────────────┐ │
│  │                        Provider Abstraction                         │ │
│  │            统一接口：sendMessage() / streamResponse()               │ │
│  └──────────────────────────────────┬──────────────────────────────────┘ │
└─────────────────────────────────────┼──────────────────────────────────────┘
                                      │
                                      │ Tool Calling
                                      │
┌─────────────────────────────────────▼──────────────────────────────────────┐
│                      第5层：工具与技能层 (Tools & Skills Layer)               │
│                                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐       │
│  │   数据库    │  │   浏览器    │  │   文件系统  │  │   代码执行  │       │
│  │  query_db   │  │  browser    │  │  file_ops   │  │   execute   │       │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘       │
│         │                │                │                │              │
│  ┌──────▼────────────────▼────────────────▼────────────────▼──────┐       │
│  │                     Skills Registry                            │       │
│  │  • 技能发现  • 动态加载  • 版本管理  • 权限控制                 │       │
│  └────────────────────────────────────────────────────────────────┘       │
└──────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ 响应数据
                                      │
┌─────────────────────────────────────▼──────────────────────────────────────┐
│                      第6层：响应处理层 (Response Layer)                        │
│                                                                              │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐          │
│  │   内容生成       │  │   格式化引擎     │  │   模板渲染       │          │
│  │  (LLM Output)    │──► (Markdown/HTML) │──►  (渠道特定)     │          │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘          │
└──────────────────────────────────────────────────────────────────────────┘
                                      │
                                      │
┌─────────────────────────────────────┼──────────────────────────────────────┐
│                      第7层：数据持久层 (Persistence Layer)                      │
│                                     │                                        │
│  ┌──────────────────────────────────┼──────────────────────────────────┐   │
│  │                                  │                                  │   │
│  │  ┌──────────────┐  ┌─────────────▼──────────────┐  ┌──────────────┐ │   │
│  │  │   JSON 文件  │  │    SQLite Database         │  │   向量存储   │ │   │
│  │  │  (配置/日志) │  │  (会话/记忆/审计)          │  │  (语义搜索)  │ │   │
│  │  └──────────────┘  └────────────────────────────┘  └──────────────┘ │   │
│  │                                                                    │   │
│  └────────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 各层职责

| 层级 | 名称 | 核心职责 | 关键技术 |
|------|------|---------|---------|
| 第1层 | 用户接入层 | 接收多平台消息 | WhatsApp API、钉钉 Webhook |
| 第2层 | 网关层 | 统一接入、认证、路由 | Express、WebSocket、JWT |
| 第3层 | 核心服务层 | 会话管理、记忆、定时任务 | SQLite、Node-Cron |
| 第4层 | AI 引擎层 | 大模型推理、意图识别 | OpenAI API、Claude API |
| 第5层 | 工具技能层 | 执行具体操作 | 数据库驱动、Playwright |
| 第6层 | 响应处理层 | 生成并格式化回复 | Markdown、模板引擎 |
| 第7层 | 数据持久层 | 保存历史记录和记忆 | SQLite、向量数据库 |

---

## 三、核心组件详解

### 3.1 Gateway 网关（架构核心）

**为什么需要 Gateway？**

1. **安全隔离**：只暴露 Gateway，内部服务不直接对外
2. **协议统一**：将各平台不同格式转为统一 Message
3. **进程隔离**：Gateway 独立运行，崩溃不影响核心

**技术实现**：
```typescript
{
  httpServer: Express,      // REST API
  wsServer: WebSocket,      // 实时通信
  auth: JWT + Whitelist,    // 认证层
  router: MessageRouter     // 路由分发
}
```

**端口**：默认 18789（本地回环）

### 3.2 Session 会话管理

**三层记忆架构**：

```
Memory System
├── 短期记忆（Short-term）
│   └─ 最近 10-20 轮对话，保存在内存
├── 长期记忆（Long-term）  
│   └─ 用户偏好、历史约定，保存在 SQLite
└── 语义记忆（Semantic）
    └─ 向量化的历史内容，支持模糊搜索
```

### 3.3 Tools 工具系统

**Skill 定义**：
```typescript
interface Skill {
  name: string;
  description: string;
  tools: Tool[];
  handlers: Handler[];
  permissions: string[];
}
```

**加载机制**：
- 动态加载：运行时从 `skills/` 目录加载
- 热更新：修改 Skill 无需重启主程序
- 依赖注入：自动注入所需服务

---

## 四、完整数据流

### 4.1 时序图（以"查询销售额"为例）

```
T+0ms      用户在微信发送："今天销售额多少？"
           ↓
T+100ms    Gateway 层：认证 → 路由 → 分发到 Session
           ↓
T+300ms    核心层：加载上下文 + 三层记忆
           ↓
T+500ms    AI 层：推理 → 意图识别 → 选择 Tool
           ↓
T+1500ms   工具层：执行 SQL 查询 → 获取结果
           ↓
T+2000ms   响应层：生成自然语言回复
           ↓
T+2500ms   输出层：发送用户 + 保存历史

总耗时：约 3 秒
```

### 4.2 数据流转抽象

```
用户输入
    ↓
1. 接入层：多渠道 → 统一 Message
    ↓
2. 网关层：认证 → 路由 → 分发
    ↓
3. 核心层：加载上下文 + 记忆
    ↓
4. AI层：推理 → Tool选择 → 执行
    ↓
5. 响应层：生成回复 → 格式化
    ↓
6. 输出层：发送用户 + 保存
```

---

## 五、与 OpenCode 架构对比

### 5.1 项目背景

**OpenCode**（https://github.com/anomalyco/opencode）是另一个开源 AI 编程助手，与 OpenClaw 有本质区别：

| 维度 | **OpenClaw** | **OpenCode** |
|------|--------------|--------------|
| **定位** | 企业级 AI 助手平台 | 个人开发者 AI 工具 |
| **场景** | 办公自动化、数据监控、客服 | 代码编写、调试、重构 |
| **用户** | 团队/全员 | 个人开发者 |
| **部署** | 服务器常驻 | 本地即用即走 |

### 5.2 技术栈对比

| 维度 | **OpenClaw** | **OpenCode** |
|------|--------------|--------------|
| **语言** | TypeScript | TypeScript |
| **运行时** | Node.js 22+ | Bun 1.3.5 |
| **架构** | 多进程服务 | Monorepo（多包） |
| **UI** | 无（通过通讯软件） | SolidJS + OpenTUI |
| **AI SDK** | 直接调用 API | Vercel AI SDK |
| **进程模型** | 多进程（Gateway+Agent） | 单进程（CLI工具） |

### 5.3 架构差异详解

#### 进程模型差异

**OpenClaw - 多进程**：
```
Gateway进程 ──┐
              ├── IPC通信 ──→ Agent进程
Cron进程 ─────┘
```

- Gateway 与 Agent 分离
- Gateway 崩溃不影响业务
- 支持多 Agent 负载均衡

**OpenCode - 单进程**：
```
单进程内:
├── Main Goroutine（主循环）
├── TUI Goroutine（界面渲染）
├── LSP Goroutine（代码分析）
└── AI Goroutine（API调用）
```

- 启动快（毫秒级）
- 资源占用低
- 适合个人开发机

#### 架构模式差异

**OpenClaw - 平台化架构**：
```
用户层（多平台）
    ↓
Gateway（统一接入）
    ↓
Agent（业务处理）
    ↓
Tools（可扩展）
```
- 像操作系统一样管理多个 Agent
- 服务端常驻，24小时在线
- 多用户同时服务

**OpenCode - 工具化架构**：
```
终端界面（TUI）
    ↓
核心逻辑（Monorepo）
    ↓
AI SDK（Vercel）
    ↓
Tools（内置）
```
- 像高级编辑器插件
- 即用即走，不常驻
- 单用户使用

#### 交互方式差异

**OpenClaw - 异步消息**：
- 用户在微信/钉钉发消息
- OpenClaw 异步处理
- 回复推送到用户手机
- 适合：客服、监控、定时任务

**OpenCode - 同步交互**：
- 用户在终端打开 OpenCode
- 实时交互，即时响应
- 退出即结束会话
- 适合：编码、调试、代码审查

### 5.4 工具系统对比

**OpenClaw Tools**（面向办公自动化）：
```typescript
- query_database    // 查询数据库
- web_search        // 网页搜索
- browser           // 浏览器自动化
- send_email        // 发送邮件
- send_whatsapp     // 发 WhatsApp
- send_dingtalk     // 发钉钉
```

**OpenCode Tools**（面向代码开发）：
```typescript
- read_file         // 读取文件
- edit_file         // 编辑文件
- search_files      // 文件搜索
- execute_command   // 执行命令
- git_diff          // Git 差异
- git_commit        // Git 提交
```

### 5.5 存储方案对比

**OpenClaw**：
```
存储层
├── SQLite（会话、记忆）
├── JSON（配置）
└── 向量库（语义搜索）
```

**OpenCode**：
```
存储层
├── SQLite（对话历史）
└── 文件系统（配置、代码缓存）
```

**差异**：
- OpenClaw：三层记忆（短期/长期/语义）
- OpenCode：单层对话历史

### 5.6 适用场景对比

| 场景 | OpenClaw | OpenCode |
|------|----------|----------|
| 智能客服 | ✅ 多平台接入 | ❌ 不支持 |
| 数据监控 | ✅ 定时任务 | ❌ 不支持 |
| 自动化报告 | ✅ 自动发送 | ❌ 不支持 |
| 代码编写 | ⚠️ 基础支持 | ✅ 专业支持 |
| 代码重构 | ⚠️ 基础支持 | ✅ 专业支持 |
| 调试辅助 | ⚠️ 基础支持 | ✅ 专业支持 |

### 5.7 OpenCode Server 模式详解

**重要发现**：OpenCode 也有 **Server 模式**，通过 `opencode server` 命令启动。

#### Server 模式特点

**架构设计**：
```
┌─────────────────────────────────────────────────────────────┐
│                     OpenCode Server                         │
│                     （HTTP API Server）                      │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   HTTP API   │  │  WebSocket   │  │  SSE Events  │      │
│  │   (Hono)     │  │              │  │              │      │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘      │
│         │                  │                  │             │
│         └──────────────────┼──────────────────┘             │
│                            │                                │
│  ┌─────────────────────────▼──────────────────────────┐    │
│  │                 API Routes                         │    │
│  │  /project  /session  /file  /config  /agent       │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
          │
          │ 远程连接
          ▼
┌─────────────────────┐  ┌─────────────────────┐
│      TUI Client     │  │    Web Client       │
│   (终端界面)         │  │   (浏览器/移动端)    │
└─────────────────────┘  └─────────────────────┘
```

**技术实现**：
- **框架**：Hono（轻量级 Web 框架）
- **传输**：HTTP + WebSocket + SSE（Server-Sent Events）
- **默认端口**：4096
- **运行时**：Bun.serve（原生支持）

**核心功能**：

| 功能 | 说明 |
|------|------|
| **REST API** | 完整的 HTTP API（/project, /session, /file 等） |
| **WebSocket** | 支持实时双向通信 |
| **SSE** | 服务器推送事件流（实时更新） |
| **CORS** | 可配置跨域，支持 Web 客户端 |
| **Basic Auth** | 用户名/密码认证保护 |
| **mDNS** | 自动发现服务（局域网内） |

**与默认模式的区别**：

| 模式 | 启动命令 | 特点 | 适用场景 |
|------|---------|------|---------|
| **TUI 模式**（默认） | `opencode` | 本地终端交互，单用户 | 个人开发 |
| **Server 模式** | `opencode server` | HTTP API 服务，多客户端 | 远程开发、团队协作 |

**Server 模式的意义**：
1. **远程开发**：在远程服务器运行 OpenCode，本地通过浏览器/移动端访问
2. **多客户端**：TUI 只是其中一个客户端，还可以有 Web、Mobile App
3. **架构灵活**：Client/Server 分离，类似 VS Code 的 Remote 开发

**启动示例**：
```bash
# 启动 Server 模式
opencode server --port 4096 --hostname 0.0.0.0

# 远程连接
# 在浏览器打开 http://localhost:4096
# 或使用 TUI 客户端连接远程 Server
```

#### OpenClaw vs OpenCode Server 模式对比

| 维度 | OpenClaw | OpenCode Server |
|------|----------|-----------------|
| **设计目标** | 多平台通讯集成 | 代码开发能力暴露 |
| **接入方式** | 微信/钉钉/邮件 | HTTP API/WebSocket |
| **默认端口** | 18789 | 4096 |
| **消息协议** | 专有 Message 格式 | REST API + SSE |
| **客户端** | 聊天软件（被动） | TUI/Web（主动连接） |
| **交互模式** | 异步消息推送 | 同步请求响应 |
| **用户感知** | 像聊天机器人 | 像开发工具服务 |

**关键差异**：
- **OpenClaw**：侧重"多平台接入"，用户在微信/钉钉内与 AI 对话
- **OpenCode Server**：侧重"开发能力暴露"，提供 API 供其他客户端调用

### 5.8 总结：核心差异

| 维度 | OpenClaw | OpenCode |
|------|----------|----------|
| **设计目标** | 企业自动化平台 | 开发者生产力工具 |
| **运行模式** | 服务端常驻 | 客户端/Server 双模式 |
| **交互方式** | 异步消息（聊天） | 同步 TUI/API |
| **用户数量** | 多用户 | 单用户（Server可多客户端） |
| **部署位置** | 服务器 | 本地/服务器皆可 |
| **扩展方式** | Skills 插件 | 内置工具 + API |

---

## 六、技术选型建议

### 选择 OpenClaw，如果：

- ✅ 需要**服务端常驻**，24小时在线
- ✅ 需要**多平台通讯**（微信/钉钉/邮件）
- ✅ 需要**团队协作**，多人使用
- ✅ 需要**定时任务**和自动化
- ✅ 需要**数据留存在服务器**

**典型场景**：企业智能客服、数据监控报警、自动化报告

### 选择 OpenCode，如果：

- ✅ **个人开发者**使用
- ✅ 专注**代码编写**和调试
- ✅ 需要**IDE 级别的交互**
- ✅ 追求**轻量快速启动**

**典型场景**：日常编码开发、代码重构审查、学习新技术

### 两者都用：

- **OpenClaw** 部署在服务器：处理团队自动化、监控、客服
- **OpenCode** 安装在开发机：辅助日常编码工作

---

## 七、架构设计亮点总结

### OpenClaw 核心亮点

1. **多进程设计**：Gateway 与 Agent 分离，稳定性高
2. **三层记忆**：短期/长期/语义，个性化程度高
3. **多平台接入**：一次开发，多渠道使用
4. **Skills 热插拔**：动态加载，不停机更新
5. **数据本地**：完全私有化部署

### 与 OpenCode 的本质区别

虽然都是 AI 助手，但**完全不同的产品**：
- **OpenClaw** = 企业自动化平台（RPA + AI）
- **OpenCode** = 开发者生产力工具（Cursor 竞品）

**选择依据**：看你的主要需求是"自动化办公"还是"代码开发"

---

**文档结束**

*本文档重点介绍 OpenClaw 架构，同时对比 OpenCode 帮助理解两者差异*
