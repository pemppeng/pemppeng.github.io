# OpenClaw 与 Crush（原 OpenCode）架构对比

**重要说明**：OpenCode 项目已归档，现由 **Crush** 项目继续开发（https://github.com/charmbracelet/crush）

**文档日期**：2026年2月2日  
**目的**：准确对比两个项目的底层架构差异

---

## 一、项目状态澄清

### OpenCode → Crush 的变迁

```
2024年    OpenCode 首次发布（Go语言，终端AI助手）
    ↓
2025年中  OpenCode 归档（不再维护）
    ↓
2025年底  项目迁移至 Crush（charmbracelet/crush）
          由原作者 + Charm 团队继续开发
```

**当前状态**：
- **OpenCode**：已归档（https://github.com/opencode-ai/opencode）❌
- **Crush**：活跃开发中（https://github.com/charmbracelet/crush）✅

本文对比的是 **OpenClaw** vs **Crush**（而非旧版 OpenCode）

---

## 二、核心差异总览

| 维度 | **OpenClaw** | **Crush** |
|------|--------------|-----------|
| **编程语言** | TypeScript / Node.js | Go |
| **运行环境** | 服务器/云端（常驻进程） | 本地终端（CLI工具） |
| **交互方式** | 微信/钉钉/多平台聊天 | 终端命令行（TUI） |
| **架构模式** | 多进程微服务（Gateway分离） | 单进程应用 |
| **存储方案** | SQLite + JSON + 向量库 | SQLite |
| **目标用户** | 企业/团队（全员使用） | 开发者（个人使用） |
| **核心场景** | 自动化办公、数据监控、客服 | 代码编写、调试、重构 |
| **AI集成** | 多Provider（Kimi/Claude/GPT） | 多Provider |
| **开源状态** | ✅ 开源 | ✅ 开源 |

---

## 三、OpenClaw 架构详解

### 3.1 技术栈

```yaml
语言: TypeScript
运行时: Node.js 22+
架构: 多进程微服务
进程模型:
  - Gateway进程（端口18789）
  - Agent进程（核心业务）
  - 可选：Cron进程（定时任务）
存储:
  - SQLite（会话、记忆）
  - JSON（配置）
  - 向量数据库（语义搜索）
通讯:
  - WebSocket（实时）
  - HTTP REST API
  - IPC（进程间通信）
```

### 3.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户接入层                            │
│  微信   钉钉   Telegram   Discord   邮件                     │
└───────────────────────────┬─────────────────────────────────┘
                            │ Webhook/WebSocket
┌───────────────────────────▼─────────────────────────────────┐
│                      Gateway进程                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  HTTP Server │  │  WebSocket   │  │   认证路由   │      │
│  │  (Port 18789)│  │   Server     │  │   (JWT)      │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└───────────────────────────┬─────────────────────────────────┘
                            │ IPC (Unix Socket)
┌───────────────────────────▼─────────────────────────────────┐
│                      Agent进程                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Session    │  │    Memory    │  │    Tools     │      │
│  │   Manager    │  │    System    │  │   (Skills)   │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                            │                                 │
│  ┌─────────────────────────▼──────────────────────────┐     │
│  │                 AI Provider                        │     │
│  │  Kimi API   Claude API   GPT API   Local Models   │     │
│  └────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 核心特点

**多进程设计**：
- Gateway 与 Agent 分离
- Gateway 崩溃不影响业务
- 支持多 Agent 负载均衡

**多平台接入**：
- 同时服务多个聊天平台
- 统一消息格式
- 渠道适配器模式

**三层记忆**：
- 短期记忆（对话上下文）
- 长期记忆（用户偏好）
- 语义记忆（向量检索）

---

## 四、Crush 架构详解

### 4.1 技术栈

```yaml
语言: Go
运行时: Go 1.24+
架构: 单进程终端应用
进程模型:
  - 单进程多 Goroutine
  - 主线程 + TUI渲染线程 + 后台任务线程
存储:
  - SQLite（会话、配置）
  - 文件系统（代码文件）
UI框架:
  - Bubble Tea（TUI框架）
  - Charmbracelet 生态
  - Lipgloss（样式）
通讯:
  - 本地STDIO
  - LSP（Language Server Protocol）
```

### 4.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                      终端界面层                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  Bubble Tea TUI                      │   │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────┐  │   │
│  │  │  对话列表    │  │   代码编辑   │  │  文件树  │  │   │
│  │  │              │  │              │  │          │  │   │
│  │  └──────────────┘  └──────────────┘  └──────────┘  │   │
│  │                    ┌──────────────┐                │   │
│  │                    │   输入框     │                │   │
│  │                    └──────────────┘                │   │
│  └─────────────────────────────────────────────────────┘   │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                      核心应用层                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   Session    │  │   LSP Client │  │    Tools     │      │
│  │   Manager    │  │   (代码分析) │  │  (文件操作)  │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
│                            │                                 │
│  ┌─────────────────────────▼──────────────────────────┐     │
│  │                 AI Provider                        │     │
│  │  Claude API   GPT API   Gemini   OpenRouter...    │     │
│  └────────────────────────────────────────────────────┘     │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 核心特点

**Bubble Tea TUI**：
- 声明式 UI 框架
- 响应式布局
- 类似 Elm 的 MVU 模式

**LSP 集成**：
- Language Server Protocol
- 代码补全、跳转、诊断
- 多语言支持

**单进程模型**：
- 启动快（毫秒级）
- 资源占用低
- 适合个人开发机

---

## 五、架构对比分析

### 5.1 进程模型对比

| 特性 | OpenClaw | Crush |
|------|----------|-------|
| **进程数** | 多进程（2+） | 单进程 |
| **启动速度** | 较慢（秒级） | 快（毫秒级） |
| **内存占用** | 较高（100MB+） | 低（10-30MB） |
| **容错性** | 高（进程隔离） | 中（单点故障） |
| **扩展性** | 水平扩展（多Agent） | 垂直扩展（有限） |

**OpenClaw 多进程**：
```
Gateway进程 ──┐
              ├── IPC通信 ──→ Agent进程
Cron进程 ─────┘
```

**Crush 单进程**：
```
单进程内:
├── Main Goroutine（主循环）
├── TUI Goroutine（界面渲染）
├── LSP Goroutine（代码分析）
└── AI Goroutine（API调用）
```

### 5.2 存储方案对比

| 特性 | OpenClaw | Crush |
|------|----------|-------|
| **会话存储** | SQLite | SQLite |
| **配置格式** | JSON | YAML/JSON |
| **记忆系统** | 三层（短期/长期/语义） | 单层（对话历史） |
| **向量检索** | 支持（ChromaDB） | 不支持 |
| **文件存储** | 本地文件系统 | 本地文件系统 |

**OpenClaw 记忆架构**：
```
Memory System
├── Short-term（内存队列，20轮）
├── Long-term（SQLite，永久）
└── Semantic（向量库，语义搜索）
```

**Crush 记忆架构**：
```
SQLite Database
└── conversations表（对话记录）
```

### 5.3 AI 集成对比

| 特性 | OpenClaw | Crush |
|------|----------|-------|
| **Provider支持** | Kimi/Claude/GPT/本地 | Claude/GPT/Gemini/多供应商 |
| **调用方式** | HTTP API | HTTP API |
| **流式响应** | 支持 | 支持 |
| **Tool调用** | 支持（Skills） | 支持（Tools） |
| **多模态** | 文本/图片 | 文本/图片 |

**相同点**：
- 都使用 Provider 模式抽象不同 AI
- 都支持流式输出（打字机效果）
- 都支持 Function Calling/Tool Use

### 5.4 工具系统对比

**OpenClaw Tools**：
```typescript
// Skill-based，可动态加载
interface Skill {
  name: string;
  tools: Tool[];
  handlers: Handler[];
}

// 示例工具
- query_database  // 查询数据库
- web_search      // 网页搜索
- send_email      // 发送邮件
- browser         // 浏览器自动化
```

**Crush Tools**：
```go
// 内置工具，代码操作为主
- ReadFile        // 读取文件
- WriteFile       // 写入文件
- SearchFiles     // 文件搜索
- ExecuteCommand  // 执行命令
- GitDiff         // Git差异
```

**差异**：
- OpenClaw：工具可扩展（Skills），面向办公自动化
- Crush：工具固定，面向代码开发

---

## 六、适用场景对比

### 6.1 OpenClaw 适合

- **企业自动化**：定时任务、数据监控、报告生成
- **智能客服**：多平台接入、自动回复、转人工
- **团队协作**：消息同步、日程管理、知识库问答
- **数据监控**：服务器监控、异常报警、业务指标追踪

**典型用户**：
- 企业 IT 管理员
- 运营团队
- 产品经理
- 需要自动化的知识工作者

### 6.2 Crush 适合

- **代码开发**：编写、重构、调试代码
- **代码审查**：AI 辅助 Code Review
- **技术学习**：理解代码库、学习新技术
- **个人效率**：终端内的 AI 助手

**典型用户**：
- 软件工程师
- 后端开发者
- DevOps 工程师
- 技术爱好者

---

## 七、技术选型建议

### 选择 OpenClaw，如果：
- 需要服务团队/全员使用
- 需要微信/钉钉等多平台接入
- 需要定时任务和自动化
- 需要数据留存在本地服务器

### 选择 Crush，如果：
- 个人开发者使用
- 习惯终端操作
- 主要用于写代码
- 追求轻量快速启动

### 两者都用：
- **OpenClaw**：部署在服务器，处理团队自动化
- **Crush**：安装在个人电脑，辅助日常编码

---

## 八、关键修正（针对之前文档的错误）

### 之前错误 ❌ → 现在修正 ✅

| 错误内容 | 正确信息 |
|---------|---------|
| OpenCode 是活跃项目 | OpenCode 已归档，现用 **Crush** |
| OpenCode 架构描述不准确 | Crush 是单进程 Go 应用，基于 Bubble Tea |
| 语言描述模糊 | 明确：OpenClaw=Node.js, Crush=Go |
| 存储描述不完整 | 补充：Crush 用 SQLite，无向量支持 |

---

## 九、总结

| 维度 | OpenClaw | Crush |
|------|----------|-------|
| **定位** | 企业级 AI 助手平台 | 个人开发者终端工具 |
| **架构** | 多进程服务端应用 | 单进程 TUI 客户端 |
| **交互** | 聊天软件（异步） | 终端界面（同步） |
| **部署** | 服务器常驻 | 本地即用即走 |
| **扩展** | Skills 插件系统 | 内置工具 |

**两者不是竞争关系，而是互补关系**：
- OpenClaw 服务团队，处理自动化和协作
- Crush 服务个人，专注代码开发效率

---

**文档结束**

*基于实际代码分析，准确反映 OpenClaw 和 Crush 的架构差异*
